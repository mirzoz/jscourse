var nombre = 'Marco'
var apellido = 'Tuzo'

var nombreMay = nombre.toUpperCase()
var apellidoMin = apellido.toLowerCase()


var primeraLetraNombre = nombre.charAt(0)
var cantidadLetrasNombre = nombre.length

var fullName = `${nombre.toLowerCase()} ${apellido.toUpperCase()}`

var str = nombre.charAt(1) + nombre.charAt(2)

var stri = nombre.substr(1, 2)

var name = nombre.substr(nombre.length - 1)